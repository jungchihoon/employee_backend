apiVersion: v1	
kind: Service	
metadata:	
  name: employee_backend
  labels:
    app: employee_backend
spec:	
  ports:	
  - port: 80	
    targetPort: 8080
    name: http
  selector:	
    app: employee_backend
  type: ClusterIP
